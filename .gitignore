node_modules/
*.log
.DS_Store
.env
.env.*

# MCP registry signing key. The public half goes in the site .well-known file;
# this half must never be committed.
.secrets/

# the registry publishing CLI, downloaded not vendored
mcp-publisher
